{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "eka.serviceAccountName" . }}
  labels:
    {{- include "eka.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
